{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "../build"
  },
  "exclude": ["**/*.spec.ts", "node_modules", "build"],
  "references": [
    {
      "path": "../shared"
    }
  ]
}
